/*
 * Il programma piu' piccolo possibile, per il confronto fra architetture.
 *
 * Non serve eseguirlo: serve guardare in cosa lo traduce il compilatore
 * quando gli si chiede ARM64 e quando gli si chiede x86-64.
 * Vedi la sezione "Due architetture su una macchina sola" nel README.
 */

#include <stdio.h>

int main(void) {
    printf("ciao\n");
    return 0;
}

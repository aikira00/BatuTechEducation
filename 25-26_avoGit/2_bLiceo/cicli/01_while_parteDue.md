## Esercizi iniziali su while

**Consegnare analisi (con particolare attenzione alla scrittura dell’algoritmo ad alto livello) + codice python**

Verificare sempre che i valori inseriti dall'utente siano validi e, nel caso non lo siano, riacquisirli fino a quando risultano validi.

8. **Progettare un algoritmo** che legga una sequenza di valori numerici fino a che la loro somma è minore di 100 e scriva la somma ottenuta e quanti sono i valori letti. **Codificare l'algoritmo in Python.**

9. **Progettare un algoritmo** che, dati due numeri, calcoli la loro moltiplicazione utilizzando solo l'operazione di addizione. **Codificare l'algoritmo in Python.**

10. **Progettare un algoritmo** che, letti due numeri interi in input dall'utente, trova quoziente intero e resto della divisione del primo numero  per il secondo numero utilizzando solo l'operazione di sottrazione. **Codificare l'algoritmo in Python.** (Es BitABit pag. 331)

11. **Progettare un algoritmo** che, dato un numero n in input dall'utente, legga n valori dall'utente e conti quanti di essi sono maggiori di numero scrivendo il risultato. **Codificare l'algoritmo in Python.**

12. **Progettare un algoritmo** che, letto un numero n in input dall'utente, stampi i numeri pari tra 1 e n. Il programma deve verificare che i dati inseriti dall'utente siano corretti e richiederli fino a quando l'utente non inserisce dati validi. **Codificare l'algoritmo in Python.** (Es BitABit pag. 330)

13. **Progettare un algoritmo** che, dato un numero n in input dall'utente, legga n numeri dall'utente e conti quanti di questi sono multipli di numero scrivendo il risultato. **Codificare l'algoritmo in Python.**

14. **Progettare un algoritmo** che, dato un numero n in input dall'utente, legga n numeri dall'utente e conti quanti sono i valori maggiori di numero, quanti i valori uguali a numero e quanti quelli minori. **Codificare l'algoritmo in Python.**
